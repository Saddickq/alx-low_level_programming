#include "main.h"

/**
 * int _isdigit - checks for digit
 *
 * return: 1 if c is a digit and 0 otherwise
 */

int _isdigit(int c)
{
	int number;
	if (number >= 0 && number < 10)
	{
		return (1);
	}
	else
	{
		return (0);
	}
}
